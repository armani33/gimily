<% if signed_in? %>
  <div class="behind-navbar"></div>
<% end %>
<div class="cards-wrapper">
  <% @items.each do |item| %>
    <div class="cards-container">
      <div class="card-nav">
        <%= link_to root_path do %>
          <%= image_tag("heart-bookmark.png", class: "heart-bookmark") %>
        <% end %>
        <%= link_to item.page_url, class: "url-item" do %>
          <div class="link-page">
            <p>Website</p>
            <%= image_tag("website_link.png", class: "website_link") %>
          </div>
        <% end %>
      </div>
      <div class="card-body">
        <div class="price-tag">
          <p><%= item.price %>$</p>
        </div>
        <div>
          <%= image_tag(item.images, class: "item-images") %>
        </div>
      </div>
      <div class="card-footer">
        <div class="footer-link">
          <%= link_to root_path, class: "comment-button" do %>
            <%= image_tag("messages-card-blue.png", alt: "comment", class: "icon-footer-comment")%>
          <% end %>
        </div>
        <div class="footer-link">
          <%= link_to root_path, class: "cart-button" do %>
            <%= image_tag("box_blue.png", alt: "box-cart", class: "icon-footer-box")%>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>
